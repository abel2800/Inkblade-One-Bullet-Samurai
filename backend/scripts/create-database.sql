-- SQL script to create the database manually
-- Run this in psql or pgAdmin

-- Create database (if it doesn't exist)
CREATE DATABASE inkblade_db;

-- Note: After creating the database, run:
-- npm run migrate
-- This will automatically create all tables, indexes, and relationships

